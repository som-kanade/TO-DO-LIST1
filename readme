This is simple TO-DO List built using HTML/CSS/JS

1.A Input field where the tasks to be added to list can be entered
2. when add-task button is clicked the tasks can be added to list.
3. by clicking on close(X)  button the task can be removed from list